# 泡沫雷达 — 生产环境部署配置

# 数据库（生产环境使用 PostgreSQL 或 SQLite）
# 如果使用 SQLite（简单方案）：
DATABASE_URL=sqlite+aiosqlite:///app/data/bubbleradar.db
SYNC_DATABASE_URL=sqlite:///app/data/bubbleradar.db

# Redis（生产环境需要）
REDIS_URL=redis://redis:6379/0

# API Keys（请替换为你自己的真实密钥）
FRED_API_KEY=your-fred-api-key
ALPHA_VANTAGE_API_KEY=your-alphavantage-api-key
OPENAI_API_KEY=your-openai-api-key

# 安全与 CORS（生产环境必须修改！）
SECRET_KEY=change-me-to-a-random-string-32-chars-long
API_KEY=
CORS_ORIGINS=["https://你的前端域名.com"]
ENVIRONMENT=production

# 告警推送（按需填写，留空则禁用）
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
ALERT_EMAIL_TO=
SERVERCHAN_SENDKEY=
WECHAT_WORK_WEBHOOK=
